---
description: Start a new feature from a plain-language idea. Classifies the lane, seeds the manifest, and pauses for your confirmation before writing anything.
agent: intake-analyst
subtask: true
---

Run the intake workflow for the following feature idea: $ARGUMENTS

Derive the slug and title, classify the lane using the lane-classifier skill, seed the problem section from what the idea provides, then present the lane confirmation gate and wait for the user to respond before writing anything to disk.

The idea is: $ARGUMENTS
